services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: habitat-backend
    env_file:
      - ./backend/.env
    ports:
      - "5000:5000"
    environment:
      FLASK_APP: app.py
    volumes:
      - ./backend:/app
    command: ["python", "app.py"]
    restart: always
    networks:
      - habitat-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: habitat-frontend
    working_dir: /app
    volumes:
      - ./frontend:/app:cached
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - CHOKIDAR_INTERVAL=100
      - WATCHPACK_POLLING=true
    command:
      [
        "sh",
        "-c",
        "npm install && (chmod +x node_modules/.bin/vite node_modules/esbuild/bin/esbuild node_modules/@esbuild/linux-x64/bin/esbuild 2>/dev/null || true) && npm run dev",
      ]
    depends_on:
      - backend
    networks:
      - habitat-network

networks:
  habitat-network:
    driver: bridge
